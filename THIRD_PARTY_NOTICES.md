# 第三方组件声明

本项目使用了以下第三方开源组件：

- `repak` 0.2.3，版权归其贡献者所有，采用 Apache-2.0 或 MIT 双重许可。完整的许可证原文随项目提供，位于 `resources/bin/LICENSE-APACHE` 和 `resources/bin/LICENSE-MIT`。

- `fontTools` 4.63.0，版权归其贡献者所有，采用 MIT License。该组件已打包至 `palfont-font-helper.exe` 中。

- PyInstaller 6.22.2 及其运行时 Bootloader，采用 GPL-2.0-or-later，并附有允许分发由 PyInstaller 打包生成的应用程序的特殊例外条款。

## 用户字体

用户选择的字体仅在本地进行处理。

用户应自行确认所选字体的许可证是否允许相应的使用、处理及生成字体 Mod。字体文件的授权与使用责任由用户自行承担。
